<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" type="text/css" href="/css/main.css" />
  <title>Register</title>
</head>

<body>
  <%- include('partials/navbar', {page: '/register' }) %>

    <div class="container mx-auto my-5">
      <div class="hero rounded hero-lg bg-base-200 bg-cover bg-center"
        style="background-image: url(/images/944250.jpg)">
        <div class="hero-overlay bg-opacity-60"></div>
        <div class="hero-content text-center m-6">
          <div class="max-w-md">
            <h1 class="text-5xl font-bold">Register</h1>
            <p class="py-6">Please provide your credentials below</p>
          </div>
        </div>
      </div>

      <div>
        <form class="registerForm" action="/register" method="POST">
          <div class="form-control mt-5">
            <label for="userName">Username</label>
            <input class="input input-bordered input-primary w-full max-w" type="text" id="userName" name="userName"
              placeholder="User Name" required />
          </div>
          <div class="form-control mt-5">
            <label for="password">Password</label>
            <input class="input input-bordered input-primary w-full max-w" type="password" id="password" name="password"
              placeholder="Password" required />
          </div>
          <div class="form-control mt-5">
            <label for="confirmPassword">Confirm Password</label>
            <input class="input input-bordered input-primary w-full max-w" type="password" id="confirmPassword"
              name="password2" placeholder="Confirm Password" required />
          </div>
          <div class="form-control mt-5">
            <label for="email">Email</label>
            <input class="input input-bordered input-primary w-full max-w" type="email" id="email" name="email"
              placeholder="Email Address" required />
          </div>

          <button class="btn btn-primary mt-3" type="submit">Register</button>
        </form>

        <% if(errorMessage) { %>
          <div class="alert alert-error mt-3">
            <strong>Error:</strong>
            <%= errorMessage %>
          </div>
          <% } %>

            <% if(successMessage) { %>
              <div class="alert alert-success mt-3">
                <strong>Success:</strong>
                <%= successMessage %>
              </div>
              <br />
              <a href="/login" class="btn btn-success">Proceed to Log in</a>
              <% } %>
      </div>
    </div>
</body>

</html>