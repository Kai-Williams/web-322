<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Explore our complete Lego set collection, with themes like Technic, Classic Town, and Star Wars.">
    <title>Lego Sets Collection</title>
    <link href="/css/main.css" rel="stylesheet">
</head>
<body>
  
    <%- include('partials/navbar', { page: '/lego/sets' }) %> 

    <div class="container mt-4">
        <section class="hero p-6 text-center rounded shadow fade-in">
            <div class="max-w-md mx-auto space-y-2">
                <h1 class="text-4xl font-bold text-white">Lego Collection</h1>
                <p class="text-lg text-lightblue">Explore the complete Lego set collection below</p>
            </div>
        </section>

        <div class="flex justify-center mt-4 gap-4">
          <a href="/lego/sets?theme=Technic" class="btn">Technic</a>
          <a href="/lego/sets?theme=Ninjago" class="btn">Ninjago</a>
          <a href="/lego/sets?theme=City" class="btn">City</a>
        </div>
        

        <div class="overflow-x-auto mt-6">
            <table class="table w-full">
                <thead>
                    <tr>
                        <th>Image</th>
                        <th>Name</th>
                        <th>Theme</th>
                        <th>Year</th>
                        <th>Parts</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% sets.forEach(set => { %>
                        <tr class="hover:bg-gray-100">
                            <td>
                                <div class="card w-32 h-32 shadow">
                                    <img class="w-full h-full object-cover rounded" src="<%= set.img_url %>" alt="<%= set.name %>">
                                </div>
                            </td>
                            <td><%= set.name %></td>
                            <td><a href="/lego/sets?theme=<%= set.Theme.name %>" class="btn btn-xs"><%= set.Theme.name %></a></td>
                            <td><%= set.year %></td>
                            <td><%= set.num_parts %></td>
                            <td>
                                <a href="/lego/set/<%= set.set_num %>" class="btn">Details</a>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>
